# 验收标准

## 状态

- 状态：confirmed（P0 由用户在需求阶段确认方向；逐条结果待执行轮回填）
- P0 是否已由用户确认：是（方向与范围已确认；本轮交付物为文档+原型）
- 最近确认时间：2026-06-30

## 验收表

承接 `plan.md` 的「做」。本轮交付到"文档+原型"为止（A0~A1）；A2~A8 为代码执行轮的验收契约，预先固化避免执行层重定义。

| 编号 | 对应来源 | 验收条件 | 重要性 | 证据要求 | 状态 |
| --- | --- | --- | --- | --- | --- |
| A0 | 本轮交付 | `grill.md`/`plan.md`/`tech-design.md`/`acceptance.md`/`prototype.html` 五件齐全且自洽，无 TBD/占位 | P0 | 文件存在 + 用户评审通过 | pending（待用户评审） |
| A1 | 本轮交付 | `prototype.html` 能在浏览器打开，覆盖 4 个优化点的关键交互（稳定高亮+点击预览、顺序/名字对齐、最近使用置顶、音频粘贴）的可视化原型 | P0 | 打开截图 + 用户确认"看懂了交互" | pending |
| A2 | plan#做1,2 [C] | 在编辑器 @引用素材→保存→再次打开/切回该任务，高亮**全部保留**，无降级为纯文本 | P0 | 录屏/截图：往返前后高亮一致；构造 label 改名场景仍稳定 | pending（执行轮） |
| A3 | plan#做2 [C] | 点击高亮的图片 mention 弹出预览；点击音频 mention 可试听 | P0 | 录屏：点击→预览/播放 | pending（执行轮） |
| A4 | plan#做3 [D] | 编辑器中 mention 顺序与右侧预览顺序一致；删除某个 @mention，右侧对应项同步消失，无错位 | P0 | 截图对比 + 删除操作录屏 | pending（执行轮） |
| A5 | plan#做4 [D] | 同一素材在编辑器与右侧显示**相同主标题**(displayName)；副标题/hover 显示 asset.name | P1 | 截图：两处名字一致 | pending（执行轮） |
| A6 | plan#做5 [B] | 素材管理器默认排序把最近用过的素材置顶；引用过的老素材（created 超 3 天）也能浮顶 | P1 | 操作录屏：用过某素材后它升到顶部 | pending（执行轮） |
| A7 | plan#做6 [A] | 在提示词框粘贴/拖拽音频文件→生成音频素材，绑定 audio_asset_ids，右侧"音频"区可见且可试听 | P1 | 录屏：粘贴音频→出现→试听 | pending（执行轮） |
| A8 | plan#不做1 | **MCP 不回归**：`dreamina_queue_video` 入队的任务行为与改动前一致；MCP 入参/语义零变化 | P0 | MCP 入队一条任务，确认正常进队列、纯文本 prompt+paths 工作如常 | pending（执行轮） |
| A9 | plan#约束 | 老任务（无 prompt_doc）打开不退化（fallback 重建 ≥ 今天表现）；state.json 体积增量可接受 | P1 | 打开老任务截图 + 体积前后对比 | pending（执行轮） |

## 不作为通过依据

- 只看到代码改了、执行层自称完成、未验证截图。
- "高亮在首次编辑时正常"——必须验证**往返再编辑后**仍正常（这是病根所在）。
- 仅前端 mock，未验证 MCP 入队任务不回归。

## 待确认验收

| 编号 | 问题 | 推荐答案 | 风险 | 状态 |
| --- | --- | --- | --- | --- |
| Q-acc1 | A6 "最近使用置顶"是否要可关闭/可切回旧排序 | 暂不做开关，默认置顶即可 | 过度设计风险 | open（执行轮定） |

## 执行验收记录

> 执行完成后必须回填。本节记录最终证据，不记录运行过程态。

- 执行状态：not-started
- 执行方式：{待定，代码执行另起一轮}
- 执行时间：—
- 执行者/会话：—
- 关联提交/PR/变更摘要：—
- 详细结果文件：无（证据多时建 `result.md`）

### 验收项结果

| 编号 | 状态 | 实际证据 | 说明 |
| --- | --- | --- | --- |
| A0 | not-verified | — | 待用户评审本批文档 |
| A1 | not-verified | — | 待用户打开原型确认 |
| A2~A9 | not-verified | — | 代码执行轮回填 |

### 独立审查

- Spec review：本轮交付后由用户评审。
- Code review：执行轮进行。
- 人工验收：A2/A3/A4/A8 等 P0 必须人工确认。

### 未验证项与风险

- 富文本 doc 持久化与 `state.json` 压实目标的体积张力，需执行轮实测。
- `last_used_at` 打点若放在"入队执行时"而非"保存时"，A6 体感会变，需执行轮按 plan 推荐落地。
